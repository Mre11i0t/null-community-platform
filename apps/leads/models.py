# No new tables — this app is purely a permission-gated view layer over
# apps.events / apps.chapters / apps.notifications models, mirroring
# the original Leads:: controller namespace (chapter-leader UI).
