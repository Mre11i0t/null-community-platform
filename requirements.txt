Django==5.1.15
# mysqlclient is the production driver (requires libmysqlclient-dev at
# build time, see Dockerfile). For local dev without that system lib,
# PyMySQL is installed below as a pure-Python fallback.
pymysql==1.1.1
cryptography==49.0.0
django-debug-toolbar==4.4.6

# Auth
django-allauth==65.18.0

# REST API
djangorestframework==3.15.2
drf-spectacular==0.30.0
django-filter==24.3

# Background jobs
celery==5.4.0
redis==5.2.1
django-celery-beat==2.7.0
django-celery-results==2.5.1

# Admin
django-import-export==4.3.3

# Storage & Media
Pillow==12.3.0
django-storages==1.14.4
boto3==1.35.90

# Env & Config
django-environ==0.11.2

# Static files
whitenoise==6.12.0

# Production server
gunicorn==23.0.0

# Forms & UI
django-crispy-forms==2.3
crispy-bootstrap5==2026.3

# Security
django-recaptcha==4.0.0
django-csp==3.8
django-ratelimit==4.1.0

# Audit
django-auditlog==3.0.0

# Tagging
django-taggit==6.1.0

# Voting is first-party (apps.events.models.SessionVote) — django-vote's
# bundled migrations use Meta.index_together, removed in Django 5.1,
# making the package uninstallable on this stack.

# Utils
Markdown==3.10.2
python-slugify==8.0.4
icalendar==7.2.0
requests==2.34.2
qrcode==8.2
fido2==2.2.1
django-anymail==15.0
requests-oauthlib==2.0.0
sentry-sdk==2.64.0
PyJWT[crypto]==2.13.0
